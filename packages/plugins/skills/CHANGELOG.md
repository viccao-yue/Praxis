## 0.1.0-alpha.32 — Unreleased（2026-09-22）

- 统一管理操作与表单：复用 Harness 原生 Button/Input，Select/Textarea 共用 praxis-ui；声明原生 UI 依赖，保持主题与交互语义。

- 页面、卡片、按钮及详情/编辑弹窗改用原生主题语义颜色，支持明暗即时切换。
- 适配 DeepSeek Harness 0.1.7-alpha.1、Cordis 4.0.3，更新精确依赖。

## 0.1.0-alpha.31 — Unreleased（2026-09-20）

- 按用户决定删除能力中心「行业应用」标签入口（行业应用暂时用不到，暂无领域实现）；能力页工具栏保留专家/技能/连接器三个标签。

## 0.1.0-alpha.30 — Unreleased（2026-09-18）

- 适配 DeepSeek Harness 0.1.6-alpha.2：当前会话改由 `SessionSummary.retainedBy.mainView` 推导；打开技能会话改用官方 `uiWorkspace.openSession` 导航。

## 0.1.0-alpha.29 — 2026-09-15

- 六个内置技能统一由 `resources/skills/<name>` 维护并生成注册内容，PPT 只保留一个内置入口。
- 技能创建器接入完整的初始化、校验、资源组织和 ZIP 交付方法；弹框采用统一紧凑视觉。
- 排除 Python 缓存文件并修正当前视觉探针。

## 未发布：完整技能制作流程

- 接入 WorkBuddy 创建方法与初始化/校验/打包脚本，适配工作区草稿及 DSH 导入安装。
- 保留来源和 Apache-2.0 许可证，校验补充占位文件/失效引用，打包排除缓存。

## 当前 preview 候选（未发布）

- 六个内置技能统一为 resources/skills/<name>/SKILL.md，官方 provider 单向生成注册内容。
- 腾讯 PPT 原生 Office 适配随技能包交付，不依赖本机用户副本。

## 0.1.0-alpha.27 — 2026-09-13

- 「我安装的」升级为独立安装页视图（对齐 WorkBuddy）：市场头部入口由静态计数改为可点击按钮，进入后提供「全部技能」返回链接、标题计数、批量管理与页内「搜索已安装的技能」；进入与返回时滚动归零，焦点分别移到返回链接与入口按钮。
- 安装页复用同一套卡片渲染（renderSkillCard）、管理菜单、批量栏与卸载确认，不复制卡片逻辑；市场头部搜索、分类、可安装/已安装分区与刷新保持原行为。
- 配套 praxis-ui 0.1.0-alpha.5 新增 `back` 图标，供返回链接使用。
- “我安装的” becomes a dedicated installed-skills page (WorkBuddy-aligned): the market header entry is a real button opening a page with an “全部技能” back link, count heading, batch management and in-page search; view switches reset scroll and move focus to the back link or the entry button.
- The installed page reuses the same card renderer, management menu, batch bar and uninstall confirmation instead of duplicating card logic; the marketplace header search, categories, sections and refresh keep their existing behavior.

## 0.1.0-alpha.26 — 2026-09-13

- 技能页升级为一体化技能市场：真实分类标签、目录卡片（品牌图标、中文名称、中文说明）与“＋”直接安装；安装复用既有受管导入路径（全局名称锁、指纹复核、原子发布），不新增第二套安装器。
- 新增 开物Praxis 自有本地技能目录：`catalog.json` + `icons/` + `payloads/`，由 `scripts/build-skill-catalog.mjs` 从本地市场镜像生成；Host 只读解析元数据、图标与惰性负载，目录缺失或损坏返回真实状态与诊断，不伪造空目录；超限条目如实展示安装限制。
- Connection API 新增 `catalog`、`install-catalog` 端点与认证 `GET /api/praxis-skills/icon` 图标路由（内容哈希 revision、immutable 缓存）；图标由 Host 读取校验，浏览器不直接访问目录文件。
- 技能弹框继续复用 praxis-ui 公共 Modal 外壳，仅在插件级类名上收窄与紧凑化：目录预览 720px、技能详情 820px、64px 图标与 24px 标题；公共默认宽度与无障碍行为不变。
- 市场卡片按 WorkBuddy 列表对齐呼吸感：网格间距 22px、卡片内距 22px/圆角 18px/最小高 190px、品牌图标 46px、标题 17px、描述 15px 两行、圆形“＋”40px；修复目录预览弹框“＋ 安装”按钮被卡片圆形样式挤压致文字换行。
- 卡片高度压缩至 152px（内距 18px、描述上边距 12px），分类标签栏隐藏滚动条（保留横向滚动能力），技能弹框关闭按钮改为 40px 并与标题行垂直居中（top 28/right 32）。
- 卡片再压缩至内容自然高度 131px（内距 14px、描述上边距 6px、去掉底部空余），高宽比明显更扁，对齐 SkillHub 列表观感。
- Skill page becomes an integrated marketplace: real category tabs, catalog cards (brand icon, Chinese title, localized description) and direct "＋" install through the existing managed import path; no second installer.
- Adds the Praxis-owned local skill catalog (`catalog.json`, `icons/`, `payloads/`) generated by `scripts/build-skill-catalog.mjs`; Host reads metadata, icons and inert payloads only, and reports missing/invalid state instead of fabricating an empty catalog.
- Connection API adds `catalog` and `install-catalog` endpoints plus an authenticated `GET /api/praxis-skills/icon` route (content-hashed revision, immutable caching).
- Skill dialogs keep the shared praxis-ui Modal shell and only narrow/compact through plugin classes: 720px catalog preview, 820px detail, 64px mark and 24px title; shared defaults and accessibility behavior stay untouched.
- Marketplace cards follow the WorkBuddy list rhythm: 22px grid gap, 22px padding / 18px radius / 190px min-height, 46px brand icon, 17px title, 15px two-line description and a 40px round "＋"; fixes the catalog preview "＋ 安装" button being squeezed into the round card shape.
- Cards compress to 152px (18px padding, 12px description offset), the category tab strip hides its scrollbar while keeping horizontal scroll, and the dialog close button becomes 40px aligned to the title line (top 28 / right 32).
- Cards compress again to their natural 131px height (14px padding, 6px description offset, no leftover bottom space), matching the flatter SkillHub list rhythm.

# 0.1.0-alpha.24

## 0.1.0-alpha.25 — 2026-09-12

- 单个专家 alpha.1 配套：独立 Host 组合、共享 Skill 修订与受控任务入口。
- Companion for Experts alpha.1: standalone Host composition, shared Skill revisions and governed task entry points.


- 补齐独立 dsh.bundle/patch 与 dsh.client 浏览器产物，官方 Profile 可单独安装本包；不依赖 开物Praxis 总包或开发 workspace。
- Host/Client 分别声明依赖与生命周期，技能侧栏入口归本插件所有。取消 Client 请求、撤销 Host 路由并排空在途上传。
- 本地服务类型移至 praxis-contracts/skills，两个消费者通过注入共享服务；原有技能文件、草稿和回收数据格式保持兼容。
- 新增真实独立 tarball 浏览器验收：编辑/冲突、启停、卸载/恢复，以及移除、重装和重复安装不重复注册。

# 0.1.0-alpha.23

- 为认证 Fetch 管理请求增加明确超时，并把取消与超时映射为稳定客户端错误。
- 导入文件读取、上传、预检与确认安装共用可中止操作；界面提供取消上传和取消安装入口。
- Host 在导入遍历、内容指纹、复制和原子发布边界检查 AbortSignal；发布前取消不会产生安装目标，暂存凭据仍可重试。

# 0.1.0-alpha.22

- 增加批量启用、停用和可恢复卸载，逐项返回成功与失败并保留失败选择。
- 增加可扩展的 Host 依赖检查契约；卸载前显示依赖影响，以 revision 防止确认后依赖变化，并阻止仍有强依赖的卸载。
- 增加“受管导入含资源 → 冷启动新进程 → 官方 Skill 工具调用”的组合验收。
- 修复卡片管理菜单被相邻卡片遮挡，以及导入完成后旧弹框遮罩残留的问题。

# 0.1.0-alpha.21

- `skill-creator` 改用 Harness 官方 `defineTool` 注册的 Host 草稿、校验、发布工具；发布要求精确草稿修订和用户确认，不再指导模型直接写官方目录。
- SkillManager 持久化私有草稿，发布时重新校验、全局查重、原子写入并回读核对。
- 全局列表展示无效本地技能及可修复诊断，修复保存后重新进入官方发现和调用链。

# 0.1.0-alpha.20

- 技能导入按全局名称取得跨进程锁，并检查所有官方根、扁平文件、目录技能及其他已注册提供方，拒绝同名遮蔽。
- 暂存收据与安装副本使用完整文件内容指纹复核，拒绝预检后的同长度篡改。
- 回归覆盖跨根冲突、并发确认单胜者和 Host 重启后继续确认。

# 0.1.0-alpha.19

- 增加 `.zip`、`.md` 和文件夹的浏览器上传、Host 预检、显式确认、失败重试与过期清理。
- 上传复用 Harness Connection 的认证流式 Fetch 扩展面，安装进入官方技能根并由官方 provider/watcher 发现。

# 0.1.0-alpha.18

- 增加原作用域启停、跨进程修订锁、资源文件管理及可恢复卸载。

# 0.1.0-alpha.17

- 增加全局 Host 技能管理契约、完整详情、正文编辑和官方打开目录动作。

## 2026-09-14 PPT 风格预览候选

增加通用/红色各四套真实标题封面预览；通过 Office 持久保存及官方原生提问选择。内置 PPT 不再将红色直接视为红金；具体模板/风格及快速交付跳过选择。卡片不直接提交答案，真实模型完整制作尚待验收。
