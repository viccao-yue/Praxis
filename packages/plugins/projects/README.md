# 开物Praxis 协同空间管理

状态：**Alpha 实现中**。

协同空间工作台将指令、技能、专家、连接器、资料库资产、计划和原生 DSH 任务组织在同一协同空间中，但不复制各领域的权威数据。

首个 Alpha 提供与 WorkBuddy 对齐的协同空间中心，以及持续挂载配置侧栏和任务输入框的详情页。详情包含活动记录、计划、任务、资产四个页签；活动记录自动沉淀本机协同空间操作，不提供虚假的成员留言或通知。配置保存会生成不可变修订，新任务固定引用创建时的修订。协同空间归档需要确认，可在“已归档协同空间”中恢复且不会丢失历史。

- 实现阶段：P1
- 主任务：P1-11，详见 [开发计划](../../../docs/PLAN.md)
- 职责：协同空间说明、任务归属、资产引用及协同空间访问权限。
- 边界：协同空间不等于会话全集自动共享。

## 开发前阅读

[规则](../../../AGENTS.md)、[状态](../../../docs/STATUS.md)、[契约](../../../docs/CONTRACTS.md)、[团队设计](../../../docs/TEAM-DESIGN.md)。

所有业务操作遵守服务端主体和组织上下文；页面与 Agent 工具调用相同领域服务。可选功能接入通过公开契约与生命周期注入。

## 协同空间输入菜单

协同空间首页和协同空间会话共用＋级联菜单，选择文件、专家、技能和连接器。会话仍使用官方编辑器、上传与发送；普通会话保留原生菜单。首页选择存为草稿，创建时固定任务能力快照；会话连接器选择只影响当前任务。选择专家会确认创建新任务并复制草稿，不自动发送。菜单和已选标签跟随主题，窄屏使用可返回的单面板。

## 数据边界

协同空间资产只保存资料库 `assetId + revisionId`，正文仍由资料库管理。可选插件未安装或不可用时，界面显示诊断信息。

## 验收与下一步

完成对应 PLAN 任务及 [验收矩阵](../../../docs/ACCEPTANCE.md) 场景，记录真实测试证据后才更新状态。先验证公开接口，再实现；目前仅保留骨架，不声明加载入口、假工具或成功响应。

## 修订 6 的必做补充

详见 [协同空间设计](../../../docs/PROJECT-DESIGN.md) 和 [官方依据](../../../docs/research/workbuddy-core-domains.md)。新增目录仍为规划占位；各自实现 PLAN 的 P1 补充项并验证 J01—J10 适用项。

## 协同空间界面联动

按 [协同空间设计第 7 节](../../../docs/PROJECT-DESIGN.md) 实现本领域相关交互，验收 UI01—UI08 适用项。领域对象与协同空间关联分离，取消不提交选择，个人连接按当前主体解析。新增目录仍为 planned。

实现前必须阅读 [ADR-0007](../../../docs/adr/0007-execution-and-transfer-boundaries.md)，完成相应 B/Q 边界用例；不可只用提示词或 UI 达成权限保障。

页面及配置弹窗随 Harness 原生主题变化；文件类型图标与文档原文保留自身颜色。

### 协同空间详情体验（2026-09-22）
默认进入任务页；任务列表保持标题、来源与日期对齐，直接打开原生会话。计划表默认只读展示，点击标题或编辑按钮打开公共编辑弹窗，支持取消、保存失败保留与重试。配置区展示能力名称，活动按日期分组，窄屏配置默认收起；未开放的定时任务无添加按钮。

协同空间任务按创建时间倒序展示。新建任务经 Host 解析协同空间专用目录，并通过官方 Workspace Controller 以协同空间名登记；不会使用最近会话/首个工作区。已有任务保留原 cwd，不迁移历史文件。

协同空间输入框的“＋”使用就地级联菜单：悬停或点击专家、技能、连接器展开协同空间已有能力；空列表显示紧凑提示，添加和搜索留在同一面板。选择变更经协同空间配置修订校验保存，用于新任务；不替换运行中的会话配置。窄屏使用单面板和返回按钮。右侧协同空间配置保留完整管理入口。
