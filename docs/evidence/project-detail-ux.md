# 项目详情体验修复 P1-11

## 官方能力复用记录
- 依据：docs/dsh-v0.1.7-alpha.1/docs/web-styling.zh.md；docs/PROJECT-DESIGN.md。
- 公开入口：@deepseek-ai/dsh-client-ui-primitives@0.1.7-alpha.1，沿用 Praxis-ui Button/Input；主题由官方 ui-layout/ui-theme 拥有。
- 已有探针：scripts/probe-preview-controls.mjs；本次补充有数据的项目详情浏览器验证。
- 业务差异：项目待办、配置与任务关联归 projects；仅重排展示和按需编辑，不修改原生 Session、任务启动、引用管线。
- 验收：计划读取/编辑/取消/错误保留，任务导航、配置名称、布局与主题。结果待填写。

## 修改结果
- 项目默认任务页，计划/资产/活动记录保持独立；任务行以 SVG 图标、标题/来源、日期与打开箭头排布，移除无操作的省略号与配置 ID。
- 计划表展示文本、状态和优先级；标题/编辑按钮打开公共 Modal，原生 Input/公共 Select 编辑，取消无写入，服务端失败保留草稿供重试。看板显示全部五种状态。
- 配置栏 280px，名称取真实绑定 label，保留管理入口；定时任务未实现时仅说明，活动按日期分组。
- 输入区改为约 126px 紧凑高度，沿用原有任务启动/引用管线；页面使用官方主题语义变量，窄屏默认收起配置。

## 验收
- 项目及公共依赖构建、项目 typecheck 通过。
- 项目 10 项测试通过；旧响应式测试的 350px 下限按新 280px 设计改为 260–320px 区间，仍验证抽屉与草稿保留。
- 最终安装后浏览器通过：真实 Host持久化验证 项目四页签明暗主题、原生任务打开、1920/390 布局；临时项目验证默认阅读、取消无写入、模拟 409 失败保留、重试保存和刷新持久化。临时项目已归档。pageerror 为 0。探针 scripts/probe-project-detail-ux.mjs，证据 .artifacts/project-detail-ux/result.json 与截图；已人工检查浅色计划、深色保存结果及窄屏任务截图。
- 开发预览 18989 已安装并启动最终构建。
- 未执行真实模型任务、屏幕阅读器、所有插件全量回归；未提交、推送、发布。

## 选择框边缘裁切修复
- 官方复用仍为上述 ui-primitives 与主题 elevation；不改控件材质。项目滚动区原先横向零留白，裁掉靠边控件的外部描边与焦点环。滚动区增加 8px 横向安全区并用等量负 margin 保持内容对齐。
- 最终安装后验证通过：浅/深色任务与计划选择框左右留白至少 8px，焦点轮廓完整；390px 页面无横向溢出。截图 .artifacts/project-detail-ux/select-clip/，浏览器日志 /tmp/project-select-browser.log。项目构建与 diff 检查通过。

## 项目输入区添加菜单
官方复用记录：沿用公共 Button/主题语义和已有 AssetPicker（资料库上传/选择）、项目能力配置，不仿造模式/模型按钮或执行器。＋分类添加文件/专家/技能/连接器；@引用保持项目待办/资产/技能，空状态提供添加文件入口。能力明确为项目配置。构建与浏览器菜单/文件入口/专家配置/@/Escape 检查通过，截图 .artifacts/project-add/menu.png。确认添加后返回真实 ProjectAssetRef 生成引用标签。未执行真实上传或模型发送回归。

### 选择状态修复
复用原生主题语义变量、原生 Button 的 primary/ghost 状态与原生 checkbox；不新增主题系统。检查共用能力卡片、资料选择和引用标签的明暗主题可读性。

验证：项目构建、11 项单元测试通过；真实预览中检查专家、连接器、技能及资料卡片的 light/dark、勾选/取消和标题说明分行，截图 `.artifacts/project-selection/`。未保存选择、未触发模型。检查专家/技能/连接器/资料库其余选中 CSS，未发现同类固定深色背景加主题文字问题。

### 输入区级联选择菜单
官方复用：继续使用发布版 ui-primitives 的 Input/Button 与主题变量，保存复用项目 updateConfig 的修订校验。输入区能力入口改为锚定级联面板；已绑定列表、空状态、搜索与添加均在面板内，侧栏完整配置页保持原入口。选择仍明确作用于项目新任务，不伪装为原生专家会话切换。验收覆盖 hover/click、空态、搜索、保存失败、取消、主题和窄屏。

级联菜单验收完成：`scripts/probe-project-cascade.mjs` 通过三类空状态、无模态框、搜索、真实配置保存、409 失败与重试、light/dark、Escape、390px 返回与外部点击关闭。截图和结果 `.artifacts/project-cascade/`。测试项目已归档；未调用模型，未执行文件上传回归。构建和 11 项项目测试通过。
