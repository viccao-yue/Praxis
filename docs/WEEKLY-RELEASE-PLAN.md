# 开物Praxis 每周版本计划

制定日期：2026-09-14。首期滚动排期8周，目标每周五发布一次GitHub预览发行，周四冻结范围。时区为Asia/Shanghai。每周交付一个可验证的小闭环，不要求每周完成一个新模块。

这是目标排期，不是已完成记录，也不是定时自动发布任务。后续版本号在冻结时核对已有tag与包版本再确定；本计划不自动推送、发布或调用付费模型。实际开始开发仍按主线前置门槛登记，不恢复暂停专项。

## 发布基线与版本口径

基线为2026-09-14公开版本：experts alpha.2、skills alpha.28、activity alpha.1、office alpha.4、bundle alpha.41。详见[发行回执](releases/2026-09-14-development-candidate.md)。

每周以`2026-W38`等周次命名发行汇总；模块仍独立SemVer，只有变动模块增加版本。专家首次下一版候选为`0.1.0-alpha.3`、活动为`0.1.0-alpha.2`，最终以冻结时核对为准。新模块首个可安装切片从`0.1.0-alpha.1`起；bundle仅组合变化时升级，并记录精确依赖。只通过预览门槛继续alpha；不按日历强制转beta/rc或正式版。规则见[模块版本](MODULE-VERSIONS.md)。

## 八周目标排期

| 周次 / 目标发布日期 | 主题与交付范围 | 主要变动模块 | 必须有的验收证据 | 前置与未达标处理 |
| --- | --- | --- | --- | --- |
| W38 / 09-18 | 专家团可靠性第一批：真实成员执行、评审、同版文件交付；排查主理人常驻、成员状态滞后和实际运行故障 | experts、activity，必要时bundle | 一个真实模型完整任务；成员身份/原生子任务状态对照；交付文件与评审版本一致；普通问答回归 | 保持TM-01 in_progress，未通过整体验收不宣称专家团已收口；只发行通过检查的修复 |
| W39 / 09-25 | 专家与专家团收口：中断/取消/恢复、旧任务续作、固定修订与配备技能；逐项核对D04和TM-01验收清单 | experts、activity及必要契约 | 明确中断后的原会话恢复；取消不重复派工；前置成果保留；D04与AT-T用例逐项有通过或未完成结论 | 目标关闭当前门槛；只有规定退出项全部满足才登记完成。若未满足，下周继续当前范围 |
| W40 / 10-02 | 连接器第一切片：一个测试MCP业务系统，连接定义/实例与账号选择、连接诊断、只读查询 | connectors、业务系统示例及必要契约 | 锁定官方公开接口实测；页面/Agent使用同服务；凭据不进前端/日志/导出；跨账号不回退 | 仅在D04退出后启动D05；官方能力探针未成立时发布诊断文档或当前模块修复，不发假连接器 |
| W41 / 10-09 | 连接器受控操作闭环：查询后提交、结果核对、撤权、取消和未知写入状态处理 | connectors、必要授权/审计与bundle | 测试系统实际写入回执；未知状态先核对再重试；撤权/账号边界；真实tgz安装与停用 | 按D05原验收范围收口；不把单一测试连接宣称支持所有外部系统 |
| W42 / 10-16 | 资料库第一切片：文本/Markdown导入、列表、检索、来源和修订引用 | library及必要契约 | 导入真实文件；检索能定位来源与版本；组织/所有权授权；原文件保留 | 仅在D05退出后启动D06；不同时扩展OCR、多格式与第二套存储 |
| W43 / 10-23 | 资料复用闭环：在原生任务中使用资料、受控修改审阅、成果保存与再检索 | library、必要experts集成与bundle | 实际任务使用正确资料；写入经统一业务服务；冲突/撤权拒绝；成果再次可引用 | 逐项满足D06退出要求；模型口头引用不代替来源和保存回执 |
| W44 / 10-30 | 项目第一切片：四主标签、项目配置、真实专家/技能/资料关联、任务入口 | projects及必要契约 | 一个真实项目；能力关联校验；新任务正确绑定；旧任务组合不被静默替换 | 仅在D06退出后启动D07；不是给会话加分组，也不要求未到位应用提供方 |
| W45 / 11-06 | 项目工作闭环：待办/评论、成果引用、项目内持续工作与安装恢复 | projects及必要bundle | 待办与原生执行状态分开；成果可回溯；权限和恢复；按D07清单核对四主标签等剩余项 | 完成当前门槛后才安排行业应用；LIMS仍需独立确定首个业务切片 |

日期均为2026年。每周一根据上一周结果滚动重排；后续模块的日期是有条件目标。每次只推进一个业务主步骤，表内多个模块是其必要集成或修复，不是同时开发多个新业务插件。

## 每周工作节奏

- 周一：复盘用户反馈与上版未通过项，确定一个主目标、验收用例和明确不做的范围。新模块先登记官方复用记录及版本线。
- 周二、周三：实现与联调；修复优先于新增功能。真实模型测试显式安排并记录模型、输入、结果与成本，不以确定性夹具替代。
- 周四：冻结；构建、类型及受影响测试，实际浏览器检查，正式tgz隔离安装/升级/冷启动/停用，准备回滚和兼容说明。冻结后仅处理阻塞缺陷。
- 周五：发布通过门槛的模块及组合；公开附件回读核验SHA256，更新README、CHANGELOG、台账、真实截图与中英文宣传草稿。

每周宣传只描述实测能力，截图来自实际发行候选；动画用实际运行录屏展示，不用设计图冒充产品。外部账号写入与Twitter代发另需明确授权。

## 每周发行门槛与降级规则

1. 构建/类型与相关回归通过；涉及原生运行、权限、持久化、文件写入时，增加对应故障及安全边界检查。
2. 验收使用实际打包制品：安装和升级旧数据可读、冷启动、停用/移除后原生界面恢复。消费者契约变化同时验证；不改官方上游。
3. 发布说明列出精确模块版本、源提交、已验证场景、未验证范围、数据迁移/回滚注意事项及可下载附件摘要。仅测试过macOS就明确写macOS，不能宣称全平台。
4. 核心流程失败、数据丢失、越权或写入状态不确定而未核对时，该变动不发布。若有独立且通过门槛的修复，发布小版；若没有可交付变化，发布周报并顺延，不重复旧包或抬版本凑数。
5. 前置阶段未退出，下游新模块整体顺延。周发布节奏保留，功能目标调整；不能为了日期修改completed或扩大“已支持”的宣传。

## 保留范围与八周之后

Office现有功能的必要缺陷修复可作为当前任务前置修复，但Word新增开发、Excel格式/合并/图表扩展和Office专项仍暂停，不在周计划中自动恢复。八周不安排企业后台、自动化、多租户部署、桌面兼容、任意PDF导入/OCR或画布/多维表格；长期任务继续保留。

项目之后按既定顺序滚动安排行业应用、后台、首期全模块集成及后续团队/自动化能力。若选LIMS，应先定义样品—检验任务—结果—审核报告的最小业务范围及Host服务/Client页面/外部服务部署边界；本表不代表LIMS已立项。

## 每周发布回填模板

| 字段 | 应填写内容 |
| --- | --- |
| 周次与发行链接 | 日期、实际公开地址；未发行写原因 |
| 变动模块 | 精确版本、源提交、制品SHA256 |
| 可用变化 | 用户可复现的行为与真实截图/录屏 |
| 验收 | 用例、环境、证据路径、通过/失败/未执行 |
| 已知缺口 | 尚未支持的场景、已知问题和影响 |
| 升级与回滚 | 安装方法、数据兼容、备份/降版边界 |
| 下周调整 | 保留/顺延任务及原因，不自动推进阶段 |

本计划只制定排期。首次执行记录在W38回填；本次计划检查不等于上述任何产品验收通过。
