## 2026-09-15：官方 Team 替换自有专家团（用户明确授权）

用户要求直接废弃自有专家团执行实现。当前专项改为：移除 TeamRunsManager、SOP 运行状态机、Praxis_expert_team_* 工具和 Praxis-expert one-shot provider；以 0.1.6-alpha.1 官方 Agent Teams、九项工具及官方 Web 团队面板实现。角色/技能/WorkBuddy 导入和已发布专家内容保留为资产配置，协作场景作为工作指导，运行事实仅由官方 Session 日志和 Team 拥有。旧运行数据保留原地，不再续跑旧调度器；新任务使用官方 Team。公开查询缺陷单独实测和修复，不再作为保留旧执行器的理由。

复用：发布包 @deepseek-ai/dsh-experimental-agent-team、dsh-experimental-tool-agent-team、dsh-experimental-client-ui-agent-team；公开 agent/created、agentTeams.tryMembership 和 Agent 局部 persona/skill-filesystem 组合。已有 V1 证据包含并行、角色/技能隔离、fresh/fork、未知成员拒绝、中断和冷恢复；生产插件真实运行 9 项、独立进程恢复 2 项和七包独立 Web 7 项验证已通过，见 STATUS。保持原有资产授权，禁止复制上游实现或增加团队运行表。未验收完整真实模型业务，不对其宣称完成。

# DSH 0.1.6 升级与公开预览版计划

更新：2026-09-15。状态：隔离底座升级与部分回归已通过，继续完成兼容修复、官方新能力接入与产品验收。本文件取代对话中的早期升级方案；不代表功能已经实现或通过验收。

## 1. 目标、依据与当前状态

- 目标版本精确锁定 `@deepseek-ai/dsh@0.1.6-alpha.1`，完成升级验收后公开发布 开物Praxis 预览版，不等待上游 RC；保留上一公开预览版与经过演练的回退路径。
- 完整跟进本次官方能力。开物Praxis 自有环境默认装配已验证且适用于该运行面的能力；系统权限、浏览器、MCP 服务、SSH 连接或凭据未就绪时，使用官方可用性诊断，不能把“已装配”写成“可以执行”。保留用户显式配置。
- 专家能力按“最大限度使用官方实现、只保留必要差异”迁移，不预设保留现有 ExpertsManager、preset 编译器或协作服务。角色、指令与技能组合优先使用官方 Agent Presets/Persona/Skill；团队运行、消息、任务板和恢复归官方 Team。已有专家作品、资源和历史引用须迁移，不能因替换实现而丢失。业务 UI、WorkBuddy 包转换、专业验收与授权只保留实测仍需要的部分。替换路线先经过下述运行验证，再撤掉重复实现。
- 依据为[官方发布说明](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.6-alpha.1)、同版本 npm 包的公开 exports/types/README 和实际运行结果。遵循[官方开发规范](HARNESS-OFFICIAL-DEVELOPMENT.md)；旧文档镜像的版本差异应标明。
- 当前已完成隔离依赖安装、类型检查、构建及七层官方 Web Profile 安装启动；修复技能真实路径身份问题后，现有 integration 124/124、技能独立包和现有专家协作探针通过。详见[升级证据](evidence/dsh-0.1.6-official-integration.md)。这些结果不代表官方 Team 替换、全部新能力或真实模型业务验收通过；历史 [TM-01 探针](evidence/expert-team-tm01.md)也不能代替新版本复测。
- 实施在 `codex/dsh-0.1.6-upgrade` 分支进行，先保存并审查已有差异；用户未跟踪的 `scripts/desktop/` 不属于本专项。先在隔离 Home/Profile 和数据副本验证，再处理用户运行环境。

### 本次升级必须交付的官方新能力

用户要求是充分利用官方新特性。**升级成功同时包含“现有功能正常”和“新能力在 开物Praxis 中可用”两部分。** 下表是本次专项的工作项，不是后续愿望清单；运行包已安装、官方入口已声明、有效配置已启用、真实任务已通过分别记录。全部项当前仍待相应产品验收，不能沿用基础启动的通过结论。

| 工作项 | 用户得到的能力 | 官方所有者与 开物Praxis 接入工作 | 产品验收 |
| --- | --- | --- | --- |
| U16-F01 会话工作区 | 在任务旁开多个终端、选择 Shell、恢复归档任务，直接预览附件与成果 | 复用官方 Web 的 terminal controller、右栏终端/文件预览、Settings 归档入口；核对 开物Praxis Slot 贡献与新旧 Profile，不复制终端、会话列表或预览框架 | 认证页面完成多标签/切 Shell/刷新恢复、归档后找回、文件/Skill/交付物预览；Office 自有预览和官方预览均可正常打开，留真实截图 |
| U16-F02 官方团队执行 | 不同专家并行工作、交接、评审，用户能看到谁在做什么 | 官方 Team/Profile、`spawn_teammate`、消息与任务板负责运行；experts/activity 适配固定修订、SOP 和可见状态；已替换旧委派路径；V1—V3 剩余专业与故障场景继续验收 | A/B 不同专家与技能、并行制作、指定评审/返工/文件验收、取消及冷恢复；16 人配置按实际有效值验证 |
| U16-F03 浏览器操作 | 技能和专家可以打开网页、读取页面、填写表单并取得截图 | 使用官方 Browser Use 公开组合；分别验证 Playwright MCP、Chrome DevTools MCP、Stagehand，按配置选择后端；既有技能按实际可见工具使用，不再只靠抓取网页文本 | 本地测试站完成打开、读取、可逆表单交互、截图及取消；验证普通/专家/团队作用域；后端切换不重复注册，浏览器退出可清理 |
| U16-F04 电脑操作 | 执行网页外的应用操作与截图任务 | 使用官方 Computer Use 的 Cua Driver MCP 或原生驱动；沿用官方系统授权与审批，技能不假定所有平台都可用 | 在明确平台和驱动上完成截图、可逆应用交互、取消/退出；验证缺权限、缺驱动和失去连接的提示，不以浏览器截图代替桌面能力 |
| U16-F05 MCP 资源 | 专家按需读取 MCP 提供的文档、资源和 URI 模板 | 复用 `dsh-mcp-client`、`dsh-mcp-resources` 的调用者作用域；核对专家工具限制与共享资源工具的实际可见性，不另建 MCP 传输或连接器引擎 | 配置真实本地测试 MCP：资源列表/读取/模板、分页、无工具服务器及断连；在正确专家作用域调用成功，错误作用域不能读到资源 |
| U16-F06 远端工作区 | 本机运行 开物Praxis，在 SSH 远端读写文件和执行任务 | 通过官方远端 Fs、Shell、PTC 提供方组合；专家任务和成果路径跟随官方工作区；公开入口逐项核实，不引入另一个桌面或 SSH 执行框架 | 真实隔离 SSH 目标完成读写、命令和 PTC，确认修改的是远端文件；验证本地/远端路径、取消、断线与重连后的结果，不能静默写回本机 |
| U16-F07 无界面任务入口 | 从脚本输入任务，继续同一会话并获取结构化执行结果 | 官方 `dsh-headless` 的 stdin、`--session-id`、`--json`；只装配适用 Host 插件，提供可复现调用示例，为后续自动化插件复用保留入口 | 真实 stdin 任务生成文件，JSONL 可解析、续跑指向原会话、失败/取消退出结果正确；无 Web Client 注入依赖。不据此宣称自有定时任务模块已完成 |
| U16-F08 自动审核 | 在官方 Auto review 模式下处理操作审核 | 复用官方审核模式与权限配置；在产品现有设置/模式入口确认可发现和可切换，保持用户显式权限；不建立另一套审批服务 | 通过、拒绝、取消、审核不可用和手动模式切换均留回执；专家/团队不得绕过审核，失败不能静默放行 |
| U16-F09 长任务与代码执行 | 按任务设置代码超时，更可靠地取消和回放长任务 | 使用官方 Node PTC 独立进程、`ptc-runtime` / `workflow-ptc`、异步初始化/历史查询及 Shell/Sandbox；适配现有工作流、内置技能和保存的 preset | `run_code` 默认 120 秒/上限 600 秒按官方契约验证；准备阶段超时、取消、输出/堆限制与空 `process.env`；覆盖真实长 PPT、进程停止与资源收敛，不能只改包名 |
| U16-F10 图片与模型协议 | 图片密集任务使用官方新协议与图片复用，历史恢复保持一致 | 复用 DeepSeek Messages、Files API、图片缩放/token 估算、image offload 事件；检查旧专家 preset、官方/自定义端点、事件上报设置；保留用户地址与显式开关 | 图片上传后重复使用、工具结果回放、压缩后恢复/分叉、旧任务读取；分别记录官方端点和自定义网关结果，不以模型口述证明缓存命中或节省费用 |
| U16-F11 可见过程与连接体验 | 清楚查看执行详情，长任务计时准确，断线后能重试 | 继承官方 Trajectory 的 JSON/PTC/结果展示、推理吸顶、加号附件菜单/双语指令、会话排序/分叉、重连和长耗时展示；核对 workbench/activity/Office 是否遮挡或重复贡献 | 在 开物Praxis 页面验证 JSON 复制、PTC 结果、附件/指令、排序刷新与指定轮次分叉、手动重连和超过一小时计时；新子代理通知在 Messages 中可回放，旧保存通知单独验证 |
| U16-F12 插件与配置恢复 | 可选插件失败时其余功能可用，配置恢复结果可诊断 | 沿用官方 Loader 的必需/可选依赖与部分热更新语义；核对正式包依赖声明、失效入口和恢复后的路由；保留用户配置 | 实际触发可选/必需插件失败、无效配置与部分激活失败，修正后重试；验证服务恢复后的路由、卸载/重装和无重复监听器，不显示整批已回滚的假状态 |

**装配与迁移方式：**

- 官方默认提供的功能优先直接继承，在 开物Praxis 页面确认入口和行为；无需改动的官方组件不重写。当前发布包的 Web patch 已包含终端与归档入口，base patch 已包含 MCP Resources，但 Web 会把部分运行能力移到每个 Session 的 preset；必须核对最终 Host、Client、Session 三层组合。
- 现有 `install-preview.mjs` 只在 Profile 不存在时从官方 web 模板初始化。分别测试新安装与旧 Profile 升级，使用官方 `--dump-config`/公开 preset composition inventory 对比有效配置；不能假设旧专家保存的完整 preset 自动获得新配置。缺失的官方组合经受支持的 patch/preset 创作接口补齐，并记录变更与回退。
- Browser/Computer/SSH/MCP 等有外部条件的能力，需要提供真实可用的安装配置路径与诊断，完成适用环境实测；可选后端按矩阵逐个验证，不同时启用互相冲突的 owner。暂缺环境记为未执行并继续落实条件，不算功能完成，也不能直接从本版删掉。
- Ralph 按官方新默认关闭，保留显式启用方式；不恢复移除的 E2B。工作流按本版官方支持范围执行，不把 Python PTC 写成已支持。官方文档站 Mermaid 缩放属于文档体验，不能宣传成 开物Praxis 对话新增能力。
- 补齐官方已知边界的说明与实测：终端刷新后新视图的配色限制、MCP 不提供资源订阅且二进制结果不自动成为原生图片、旧子代理通知不会随升级改写；界面和文案不能超出实际行为。

**执行批次：**U16-2 继续协议、旧数据、长任务及 F09/F10/F12 的必要适配；U16-3 先落地 F01/F05/F11 可继承能力，再完成 F02 官方 Team 与 F03/F04/F06/F07/F08 的公开组合接入。每完成一项立即运行产品场景、记录配置与截图/文件回执，再进入下一项。所有工作项覆盖状态明确后，才进入 U16-4 制品和真实任务验收；不是仅验证 Team 后其余功能一并记为完成。

## 2. 升级后按实际结果验证官方接点

先将隔离环境升级到目标版本，沿用现有业务组合，依次跑安装、类型检查、构建、Host/Client 启动和现有功能回归。以实际错误建立适配清单，逐项定位、修改并复测；不能用静态搜索代替试运行，也不要求先完成全部 Team 迁移设计才能升级底座。无报错不等于行为正确，还须核对技能、专家、PPT、历史恢复和权限的原有行为。

下面 V1—V3 为官方团队能力的验收矩阵。用户已要求直接替换旧执行器，本轮完成核心运行与 Web 验证；未完成专业场景继续跟踪，不能恢复旧调度器。V4 随其他官方新能力接入验证。

### 验证规则

**“暂时未找到接口”是待调查状态，不是“功能无法实现”，也不能作为删功能或提前结束任务的理由。** 每项能力依次验证：官方默认组合 → 官方配置组合 → 官方公开 Service/Provider/事件/Guard/Remote 扩展。修复依赖、作用域、创建时序或调用方式问题后必须重跑失败用例。

静态类型审查只用于提出假设。使用确定性模型驱动真实 Loader、AgentLoop、Team、Session 和 Skill 服务，避免用模拟的创建成功或模型自述代替执行事实。确认接点后，再用实际模型和认证 Web Profile 验证用户流程。

验证状态使用：待验证、验证中、通过、失败待定位、当前版本公开契约限制已证实。最后一种状态必须附最小复现、全部候选路径的测试结果、官方依据和具体受限行为；不能推广为“整个功能无法实现”。遇到限制继续提出并验证官方支持的组合方案，迁移关卡保持未完成，不擅自删除目标或降低验收标准。

### U16-V1：不同专家的真实创建与绑定

先跑不依赖 开物Praxis 专家服务的官方最小组合，区分“官方默认行为”“旧业务 Guard 限制”和“确需补充的业务差异”。不能因为旧实现要求自己的 binding 而直接断定官方无法运行专家。优先验证原生角色任务与技能调用；再用公开 `agent/created`、Agent 局部作用域和技能配置验证首次请求前的角色/资源差异及冷恢复，避免为了不同角色重建队友执行器。允许共用官方运行 preset，验收关注实际行为而非旧数据结构是否照搬。

1. 在隔离的 `0.1.6-alpha.1` 官方 Team 组合创建两名已发布专家 A/B，使用不同 persona 和不同版本的测试 Skill；以 A 为 Lead，创建 B teammate。
2. 首先复跑旧探针中“成员继承 Lead preset、缺少自身 binding、首个模型请求被拒绝”的场景，记录新版本的真实结果，不把旧失败当作当前结论。
3. 分别验证 `fresh`、`fork`，检查实际子 Session、专家修订、Skill 读取结果、身份、工作区、授权和首个模型请求。不能只检查 teammate 名字或 prompt 中写着专家 B。
4. 按公开契约验证：默认 `spawn/fork` 提供方、Team 工具的 `freshProvider/forkProvider` 配置、公开 Provider 准备接口、`agent/created` 与官方 preset/技能注入时序。候选接点仅是验证对象，不提前承诺其能够改变成员 preset；不能覆盖官方 factory、访问私有字段或复制 driver。
5. 若官方组合适合保留共同运行 preset、通过公开作用域注入专家差异，也作为正式候选实测。以固定专家/技能修订、授权与可恢复性是否成立判定，不能只为保持旧内部数据结构而否定可行的官方方案。
6. 专家或 Skill 源内容在成员创建后发生修改，已有成员仍使用原绑定；无权限、伪造映射、撤权、重复创建、创建中取消均不能产生可绕行的半绑定任务。覆盖普通任务、单专家及团队三种入口的工具可见性。

**退出条件：**A/B 的实际配置和技能可区分且正确，首次请求之前完成业务准入，正常与失败路径全部有真实 Session 证据；不能关闭现有授权检查来获得“通过”。

### U16-V2：官方 Team 与业务验收协同

1. 使用官方任务依赖、owner、revision、消息和中断完成“制作 → 指定专家评审 → 返工 → 验收 → 交付”。
2. 验证 开物Praxis 的 SOP 和文件版本检查可经官方公开工具 Guard、Agent 生命周期和业务服务接入。官方 Team 拥有运行事实，开物Praxis 拥有专业评审结论和交付文件摘要；不新增调度器或第二套任务运行状态。
3. 反例必须包含：任务被标记 completed 但未评审、错误评审者、跳过前置、审核后文件变更、超出尝试预算、重复调用、取消后迟到结果、直接调用官方工具/PTC 绕过业务入口。
4. 只有真实评审与对应文件版本均有效才可签收。原生 task completed、成员 idle 或一句“已完成”均不能代替专业验收。

**退出条件：**正例成功，反例通过 Host 检查拒绝或明确使业务验收不成立；公开接点能够维持已有准入语义。没有完整证据前，不批量删除现有业务校验工具和服务。

### U16-V3：持久化、冷恢复及 UI 数据

1. 验证 running/idle/inactive/provisioning/failed，消息 accepted/queued，中断、进程退出、重启后继续执行，以及插件卸载后的停稳。独立进程能读取历史不等于能恢复执行。
2. 重启后仍能还原专家修订、Skill、主体、SOP 关联；当前撤权必须生效。相同队友和消息不会因重试被重复创建或投递。
3. UI 以官方 `agentTeams/view` 的实际成员和任务返回值为依据；分别验证 live Agent、未加载 Lead、历史会话及切换会话的行为。该 Remote 不含 mailbox，不能假定它提供完整消息和业务交接流；新增展示字段须另验对应公开查询入口。
4. 校验最终有效配置：本次 review 发现服务说明默认 16，但 `0.1.6-alpha.1` Team Profile 补丁写入 8。计划通过官方配置显式设为 16，验证 16 个不同名字可创建、第 17 个拒绝及失败成员占用名额；不能只读 release notes 判定。

**退出条件：**创建、恢复、取消、资源清理和 UI 均与官方事实一致；保存只读历史与恢复执行分别留证。

### U16-V4：其余官方能力的接点验证

下表用于确认官方组合是否可行；通过后还须完成 U16-F01—F12 中对应的产品接入、旧配置升级和实际任务验收。最小探针成功不是新功能交付完成。

| 能力 | 官方复用方向 | 必须取得的运行证据 |
| --- | --- | --- |
| Web 终端、归档与预览 | 官方 Web bundle、Terminal Controller、Sidebar/Settings 插件 | 多标签、Shell 选择、刷新恢复、归档恢复、文件及 Skill/交付物预览；无重复 Slot 注册 |
| MCP Resources | 官方 MCP Client 与 `dsh-mcp-resources` | 资源发现、读取、URI 模板、工具分页和无工具服务器；未配置/断连的真实诊断 |
| Headless | 官方 `dsh-headless` 与兼容 Host 插件 | stdin、`--session-id`、`--json`，取消与退出结果；不能把 Web Client 服务带成必需依赖 |
| SSH 工作区 | 官方远端文件、命令、Shell 和 PTC 接口 | 真实隔离 SSH 目标的读写、执行、工作区路径、取消和断连；未配置目标记为未执行 |
| Browser Use | 官方公开组合及其支持的 Playwright MCP、Chrome DevTools MCP、Stagehand 后端 | 先核对精确版本发布入口或官方 MCP 配置，再运行本地测试页的打开、读取、交互和截图；逐个验证，不把替代后端全部同时注册到同一 owner |
| Computer Use | 官方 Cua Driver MCP 或原生驱动公开组合 | 截图与可逆交互、缺权限/缺驱动诊断、退出清理；逐个记录操作系统与驱动范围 |
| Auto review | 官方公开模式及审批配置 | 审核通过、拒绝、取消、审核服务不可用和显式权限设置；不绕过官方审批所有者 |

找不到 npm 包时，继续核对官方是否通过已有包配置、MCP Server 或 Profile 组合提供能力；不能把“没有同名包”等同于“不支持”。确需外部依赖时验证安装与连接条件。只有对具体官方入口/版本限制有证据后，才能按用户已确认的范围政策将可选实验项列为本版未接入；此规则不能用于跳过 V1—V3 或降低专家团目标。

### 验证交付物与路线定稿

- 在已建立的 `docs/evidence/dsh-0.1.6-official-integration.md` 按 U16-F01—F12 分项追加命令、锁定包、公开入口、配置、断言、结果、失败定位和复现方法；不预填通过。
- 原始回执、配置摘要、无敏感信息的日志和真实 UI 截图放隔离的忽略产物目录；机器结果逐项记录 `passed/failed/not_run`，不得将跳过项计为成功。
- 复用并按新基线扩展 `scripts/probe-expert-team.mjs` 的探针资源；旧版证据单独保留，0.1.6 结果单独输出，测试不得写入用户 preview 或修改真实专家。
- V1—V3 通过后，先定稿官方复用 ADR 与专家契约映射，再实施替换；明确创建、绑定、恢复、Guard、UI 查询的实际公开接点及所需 开物Praxis 业务适配。不把搜索阶段的候选接口写成已存在的能力。

## 3. 升级实施顺序与接口变化

| 阶段 | 工作 | 进入下一阶段的条件 |
| --- | --- | --- |
| U16-0 工作区与验证环境 | 审查并保存既有试升级差异、建立分支、保留旧版制品，建立隔离 Home/Profile | 验证不影响用户运行环境，现有改动可追踪 |
| U16-1 先升级底座 | 安装精确依赖，类型检查与构建；按现有插件组合安装并启动官方 Web Profile | 底座能启动，编译、依赖和注入错误有真实复现并已修复 |
| U16-2 现有功能回归与适配 | 逐项验证普通会话、技能、专家、现有协作、Office/PPT、权限和恢复；按错误修复 API/协议/持久配置，验证旧数据及回退 | 原有功能行为回归通过，每项失败均有修复后的证据；没有报错但行为错误也纳入清单 |
| U16-3 官方新能力与 Team 迁移 | 在已升级底座执行 V1—V4，并完成 F01—F12 对应产品接入；边定位边验证官方组合，证据明确后定稿 ADR、切换 Team 运行并同步业务与 UI | V1—V3 通过后才移除旧执行适配；各项新能力有配置、用户入口/调用示例和任务回执，新增能力和业务约束联测通过 |
| U16-4 真实任务与制品验收 | 真实模型团队/PPT 任务、故障与资源生命周期回归、隔离安装包验收 | 下面的发布门禁全部通过，未验证范围准确列明 |
| U16-5 公开预览版发布 | 提升受影响模块版本、提交推送、公开制品、匿名回读校验、官网入口 | 下载制品与验收制品一致，发布及回退说明可用 |

### 底座、协议与持久配置

- DSH 依赖、overrides、锁文件和制品运行依赖统一精确版本；旧 code-runtime/workflow-worker-thread 名称迁移至官方 `ptc-runtime` / `workflow-ptc`，依据实际 exports 调整。
- 产品历史读取改用官方异步 Session Query，初始化适配串行 `agent/created`；异步 Shell/Sandbox 验证取消与准备阶段超时，Node PTC 独立进程的输出、堆限制和空环境按官方契约处理。凭据留在官方凭据/工具服务中，不批量复制进代码执行环境。
- 补齐 Office Sidebar guide 的稳定 id，核对 Host/Client 注入及效果清理；官方新增终端、设置和预览直接复用所有者。
- Messages 协议分别验证官方端点、自定义端点、旧配置、工具结果回放及图片 Files API/内联回退；不擅自重写用户网关地址。记录会话事件上报的实际官方配置、开关与保留的用户设置。
- 对已复制保存的专家 preset 盘点旧包名、工具行和校验摘要；通过官方创作接口和既有业务修订生成可追踪的兼容组合，不原地篡改已发布专家/Skill 修订。新发布与旧专家新建任务都须测试。
- 升级前先停止目标 Profile 的在途操作并备份配置、官方持久 Session、业务存储、专家/Skill 资源及清单，使用一致副本试迁移。回退使用旧代码与匹配的升级前数据快照；新版本新增数据单独留存，不让旧程序直接打开已迁移的同一 Home。

### 专家团与 UI

#### 自有代码替换清单

2026-09-15 的隔离实测确认：官方默认 Team 无需 开物Praxis binding 即可运行；公开 `agent/created` 配合 `agent.ctx.plugin()` 挂载官方 Persona/Skill Filesystem，能在共享运行 preset 下区分 A/B 队友的角色与技能，并由官方消息服务跨进程唤醒原队友。`agent.ctx.loader.create()` 本次会把 Persona 落到错误注册层，不能作为局部挂载写法。完整结果、未通过的分叉查询与边界见[证据](evidence/dsh-0.1.6-official-integration.md)。此结论支持以下替换方向，不等于已迁移用户专家。

| 现有位置 | 替换/缩减方向 | 删除前核对 |
| --- | --- | --- |
| `runtime/delegation-provider.ts`、`runtime/delegation-wait.ts` | 删除自有 one-shot 创建、等待、输出结算与续跑责任，交给官方 Team/continuable subagent | 原生取消/恢复、专业签收、用户历史迁移及生命周期通过 |
| `runtime/preset-compiler.ts` | 不再为每个专家复制整套基础运行 preset；以官方共享组合承载运行，角色与技能使用官方局部插件，仅保留作品格式转换/资源发布所需部分 | 完整专家作品、二进制资源、固定版本、首请求与重启后角色一致；本轮只验证固定测试资源，未验证发布迁移 |
| `services/experts-manager.ts` 的执行计划/委派预留与 claim/binding | 拆出并移除重复运行身份管理，使用官方 Lead/teammate/Session；只在确需的作品版本、准入和资源关联处保留薄适配 | 逐项验证官方接点能承担的校验，不为沿用旧 binding 人为阻止原生运行，也不遗漏授权或用户作品 |
| `services/team-runs.ts` 的工作依赖、成员运行与尝试控制 | 原生任务板和成员状态作为执行事实；按业务需要仅保留专业评审与文件版本签收规则 | 原生 completed 与专业验收的区别、指定评审者、返工、文件变更反例，避免留下第二份运行状态 |
| `tools/team-tools.ts` | 创建、委派、消息、等待、中断使用官方工具；旧同职责工具退出新团队组合 | 工具可见性、普通/单专家/团队入口、PTC 直调和失败返回；专业验收工具按实际职责评估，不按前缀批量保留或删除 |
| 专家制作/导入导出及活动 UI | 优先使用官方配置创作、资源与状态接口；仅保留 WorkBuddy 格式兼容和所需产品呈现 | 不丢已有专家作品；来源到官方配置可追踪；状态来自官方查询，不能将源代码减少等同于删除用户功能 |

下一验证项为固定作品映射与官方工具 Guard/专业验收；分叉历史查询异常同时以原生组合复现并验证公开读取路径。该异常已与“分叉成员可执行、可续跑”分别记账，不据此否定整条迁移路线。上述文件目前仍服务现有产品，未通过替换门槛前不先删掉运行路径。

- 通过 V1—V3 后，使用官方 Team/Profile 运行、官方 `spawn_teammate` 与其余团队工具；移除旧的运行委派路径与冲突工具注册。专业评审和文件验收的业务检查按验证方案保留并接入，不因工具名称前缀批量删除。
- 保留稳定业务专家团/运行记录标识，显式关联官方 Lead Session/Team ID、子 Session 与固定专家修订；Team ID 不覆盖既有业务主键。teammate 名称遵守官方唯一及不可复用约束，UI 使用关联后的业务显示名。
- 官方日志是 roster、mailbox 和任务执行事实的唯一来源；开物Praxis 只保存业务定义、修订、绑定和验收回执，不自建执行状态真源。
- 老任务保留可读记录，升级前的在途任务不得静默转换；续跑兼容性在数据副本上单独验证。新版不具备受支持恢复路径时给出具体迁移状态与旧环境回退方式，不能用创建新任务冒充历史恢复。
- 自有展示使用公开 Slot/Remote 和既有授权接口；官方未暴露的字段不能推测生成。认证后的真实页面截图须覆盖不同成员运行、等待、失败和重启恢复。

## 4. 验收与公开发布

- 运行 `check:versions`、`typecheck`、`build`、`check:plan`、`audit:harness-docs`、规划/集成/活动测试及升级相关探针；用精确依赖重新打包，依次验证安装、启动、卸载、重装和缺依赖/失败隔离。
- 完成一项真实模型专家团协作：两位不同专家、各自技能、指定评审、返工、最终文件交付与 UI 状态一致；完成一项真实客户模板 PPT 的导入、逐页编辑、保存、导出与视觉核对。客户资料和认证内容不进入公开制品。
- 回归普通会话、单专家、技能导入/创建、Office、PPT、长任务、会话恢复和权限检查；暂停的 Word/Excel 新功能不扩展，只做升级回归。
- 至少运行 60 分钟，同时重复团队、终端和插件创建/关闭/卸载，记录内存、文件句柄、监听器、子进程和账本错误。验收关注释放后是否收敛；一分钟或一小时未崩溃均不等于证明不存在资源泄漏。持续无界增长、`EMFILE` 或账本失败必须定位并修复后重验。
- 先完成候选制品冷启动与旧数据升级/回退，再公开发布。每个实际发生运行或依赖变化的模块使用新预览版本；不复用旧版本号，不把全部模块强制锁步。
- GitHub 创建 prerelease；Gitee 先核对官方发布/附件接口和现有权限，按该平台支持方式同步源码、标签与下载入口，不假定与 GitHub 的发布语义相同。分别记录两端结果。
- 对公开制品匿名下载回读，检查 SHA256 和清单并冷启动；失败则标记候选不可用并保留上一下载入口。README/官网只在通过后标注最新公开预览版，保留上一公开预览版及回退说明，不称为稳定版。
- Release Notes 只列实际通过的官方能力及平台范围；完整目标不自动变成已完成功能清单。核心专家团迁移关卡不能以“已记录缺口”替代通过。
- 逐项核对 F01—F12：官方复用路径、实际启用范围、旧配置处理、真实任务与失败路径、生命周期和用户文档齐全。缺外部条件与尚未执行项保持待办；“仅依赖升级”“包已安装”“最小探针通过”均不能替代产品验收，不得静默移出本次范围。

## 5. 当前下一步与状态约束

U16-0 已建分支并保存差异，U16-1 安装/类型/构建/隔离 Web 启动已通过。首轮回归发现并修复技能路径身份适配，U16-2 的现有 124 项 integration、技能安装包和协作探针通过，详见[实际证据](evidence/dsh-0.1.6-official-integration.md)。下一步继续 U16-2 的真实旧数据、协议和长任务回归，再推进 U16-3/V1—V4。运行进展与未执行项以 STATUS 的最新记录为准。

主线 D04 和 TM-01 继续保持未整体验收，本专项为用户指定的升级前置。后续 D05 等业务模块不因升级而自动开展。计划、静态接口审查、确定性运行、认证模型任务和公开制品验收分别记账；更新 [STATUS](STATUS.md) 时写明未执行项。
